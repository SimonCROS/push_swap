#!/bin/zsh

for i in $(seq $2); do
	nums=`ruby -e "puts (0..$(($1 - 1))).to_a.shuffle.join(' ')"`
	result=`./push_swap $nums | wc -l`
	if (( $result >= $3 )); then
		echo "Found $result"
		# echo "Found $result with : '$nums\`"
	fi
done
